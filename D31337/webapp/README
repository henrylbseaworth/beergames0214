This is a webapp that runs on RUBY. Make sure you have the following packages installed. 

gem install sinatra
gem install haml

To run use.. 

ruby app.rb -o 0.0.0.0 -p 4567

You should now be able to connect to the we app at your computers IP:4567
